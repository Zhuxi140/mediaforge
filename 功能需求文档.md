# MediaForge 功能需求文档

> 版本: 1.0.0
> 技术栈: Wails v2 + Go 1.23 + Vue 3 (TypeScript) + Vite + Pinia

---

## 1. 产品概述

MediaForge 是一款基于 Wails 的桌面工具集，当前提供三个核心功能模块：批量文件重命名、FFmpeg 影音格式转换、字幕提取与格式互转。所有模块通过左侧导航栏切换，文件选择与拖拽功能跨模块共享。

---

## 2. 模块一：批量文件重命名引擎

### 2.1 功能描述

支持两种重命名模式，对用户选择的多个文件生成预览并执行批量重命名。

### 2.2 界面组成

- **左侧控制面板**：模式选择 + 规则配置 + 执行按钮
- **右侧数据视图**：文件名预览表格 + 行内编辑

### 2.3 功能点

#### 2.3.1 文件管理
- 通过系统文件对话框选择文件（支持多选）
- 支持拖拽文件到右侧区域添加
- 行内快速编辑：双击/修正按钮进入行内编辑模式，支持回车保存/Esc 取消
- 移除单个文件、清空列表
- 自动去重（已添加的文件不会重复）

#### 2.3.2 基础替换模式（BasicMode）
- **前缀**：在所有文件名前添加固定字符串
- **后缀**：在所有文件名后添加固定字符串（扩展名前）
- **字符替换**：将文件名中的特定字符串替换为新字符串
- 预览时实时更新结果显示

#### 2.3.3 智能解析模式（SmartMode）
- **正则提取变量**：用户定义多个变量名 + 正则表达式，对文件名进行匹配提取
- **变量名冲突检测**：匹配失败的变量会显示缺失警告
- **模板重组**：使用 `{变量名}` 模板组装新文件名
- **干扰符过滤**：通过复选框选择要忽略的字符（空格、横线、下划线、点号、逗号）

#### 2.3.4 执行与安全性
- 预览阶段检测**同名冲突**（目标文件已存在则标记警告，拒绝覆盖）
- 预览阶段检测**格式错误**（匹配失败等）
- 执行重命名时跳过未修改的文件、冲突文件、格式错误文件
- 实际执行前展示有效操作数量（badge）

---

## 3. 模块二：FFmpeg 影音工厂

### 3.1 功能描述

基于嵌入式 FFmpeg 引擎，对视频/音频文件进行批量转码，支持硬件加速和自定义质量参数。

### 3.2 界面组成

- **左侧控制面板**：全局转码设定 + 一键转换按钮
- **右侧数据视图**：任务列表表格 + 进度展示

### 3.3 功能点

#### 3.3.1 媒体类型支持
- **视频处理**：mp4 / mkv / avi / gif（含更多格式通过 Go 端 `VideoExts` 映射校验）
- **音频提取**：mp3 / wav / aac / flac

#### 3.3.2 格式校验
- `CheckInputFile`：通过扩展名校验输入文件是否为支持的媒体格式
- 不支持的文件在任务列表直接标记为错误

#### 3.3.3 目标格式
- 视频：全局选择目标格式（mp4 / mkv / avi / gif）
- 音频：全局选择目标格式（mp3 / wav / aac / flac）
- 支持为每个任务单独覆盖目标格式

#### 3.3.4 编码加速引擎
- CPU 软解（libx264，兼容性最强）
- NVIDIA NVENC（h264_nvenc）
- Intel QSV（h264_qsv）
- AMD AMF（h264_amf）
- Apple VideoToolbox（h264_videotoolbox）

#### 3.3.5 质量参数
- **预设三档**：极速/普清 (Low) / 平衡 (Medium) / 高质量 (High)
- **自定义**：视频 CRF（0-51，推荐 18-28）或音频 VBR（0-9，推荐 0-5）

#### 3.3.6 输出路径
- 留空则输出到原文件同级目录，文件名追加 `_converted` 后缀
- 支持指定自定义输出目录

#### 3.3.7 字幕兼容性检查
- 转换 MP4 时自动检测源文件是否包含内封字幕流
- 仅检查字幕格式是否为 MP4 兼容的 `mov_text` / `tx3g`
- 不兼容时弹出模态框警告，提供"强行丢弃字幕"和"取消"选项
- `ForceDropSubtitle: true` 跳过检查直接添加 `-sn` 参数

#### 3.3.8 任务管理
- 支持多个任务同时执行（goroutine + sync.Map）
- 每个任务实时推送进度（`ffmpeg-progress-{id}`）
- 支持取消单个任务（`context.CancelFunc`）
- 成功后自动标记完成，失败时展示错误信息
- 清除失败任务、清空列表

---

## 4. 模块三：智能字幕处理中心

### 4.1 功能描述

从视频文件中提取内封字幕流（提取模式），以及在不同字幕格式之间相互转换（互转模式）。

### 4.2 界面组成

- **左侧控制面板**：模式切换 + 输出设定 + 执行按钮
- **右侧数据视图**：任务列表（含字幕流选择/勾选 UI）

### 4.3 功能点（提取模式）

#### 4.3.1 视频扫描
- 通过 FFmpeg 探测视频文件的内封字幕流
- 解析输出 `Stream #0:N` 行，提取流索引、语言、编码格式
- 展示发现的所有字幕流，每条显示语言 + 编码格式 + 多选框

#### 4.3.2 字幕流选择
- 支持勾选/取消勾选单个字幕流
- 默认全选已发现字幕
- 支持批量对 MicroDVD / DVD Subtitle 格式的预警（提示用户此类型可能提取失败）

#### 4.3.3 输出格式
- 支持输出格式：srt / ass / ssa / vtt / ttml / smi
- FFmpeg 编解码映射：
  - ass/ssa → `ass`
  - vtt → `webvtt`
  - ttml/xml/dfxp → `ttml`
  - sami/smi → `sami`
  - 默认 → `subrip`（输出 srt）

#### 4.3.4 提取执行
- 每个选中流异步提取（`-map 0:N -c:s codec`）
- 通过 Wails 事件监听完成状态
- 提取成功后标记完成数量和总数

### 4.4 功能点（互转模式）

#### 4.4.1 文件支持
- 支持的输入格式：srt / ass / ssa / vtt / txt / ttml / smi / sub
- 通过 `ValidSubtitleFormats` 映射校验

#### 4.4.2 转换配置
- 全局目标格式选择（srt / ass / vtt / ssa / ttml / smi）
- 每个任务可单独覆盖目标格式
- 可选自定义输出文件名（不填则保持原名）
- 支持指定输出目录（批量转换时推荐设置）

#### 4.4.3 安全校验
- 输出目录与原文件相同目录且未指定新文件名时，拒绝执行（防止 FFmpeg 覆盖冲突）

---

## 5. 弹窗系统

### 5.1 功能描述

自定义模态弹窗系统，支持信息/成功/警告/错误四种类型，以及确认对话框模式。

### 5.2 特性

- Promise 链式调用：`await showModal(title, message, type, isConfirm)`
- 确认对话框模式：显示"取消" + "确定/强行丢弃"按钮
- 点击模态背景遮罩关闭（非确认模式）
- 过渡动画（fade）
- 四种 SVG 图标对应不同消息类型

---

## 6. 底层系统

### 6.1 FFmpeg 引擎
- 在 `pkg/media/engine.go` 中通过 `//go:embed bin/*` 嵌入 FFmpeg 二进制文件
- `init()` 时释放到 `%APPDATA%/MediaForge/engine/ffmpeg.exe`
- 首次运行释放，后续跳过
- `localFFmpegPath` 包级变量缓存物理路径

### 6.2 任务调度
- `sync.Map` 存储活跃任务的 `context.CancelFunc`
- 任务取消：`CancelTask(id)` 调用 cancel() 并从 map 删除
- 转换完成后自动从 map 删除（`defer activeTasks.Delete`）

### 6.3 错误事件约定
- 前端通过 `EventsOn` / `EventsOff` 监听指定 ID 的事件：
  - `ffmpeg-progress-{id}`：实时进度（时间戳字符串）
  - `ffmpeg-done-{id}`：转换完成
  - `ffmpeg-error-{id}`：转换失败

### 6.4 Go 包结构

```
controller/
  renamer_app.go    # RenamerApp: 文件选择、预览、执行、快速重命名
  media_app.go      # MediaApp: 提交/取消任务、字幕扫描/提取/转换、文件校验

pkg/
  media/
    engine.go       # FFmpeg 嵌入式引擎初始化
    transcode.go    # 视频/音频转码核心逻辑、任务调度、进度监控
    subtitle.go     # 字幕兼容性检查、字幕流扫描、提取、格式转换
    types.go        # FFmpegTask、SubtitleStream、格式映射
  renamer/
    renamer.go      # GeneratePreview、ExecuteRename、QuickRenameOnDisk
```

---

## 7. 性能与限制

| 项目 | 说明 |
|------|------|
| 并发转码 | 无限制（每个任务一个 goroutine） |
| 文件大小 | 取决于 FFmpeg 和系统资源 |
| 字幕扫描 | 同步阻塞（`cmd.CombinedOutput`），大量文件时建议分批 |
| 硬件加速 | 需要对应厂商显卡及驱动 |
| 桌面平台 | 当前仅适配 Windows（`ffmpeg.exe`） |

---

## 8. 后续扩展方向（待定）

- [ ] 设置持久化（用户偏好保存）
- [ ] 日志系统（替换 `fmt.Printf`）
- [ ] 国际化支持
- [ ] FFmpeg 路径可配置
- [ ] 应用关闭时优雅停止活跃任务
- [ ] 批量重命名撤销/回滚功能
- [ ] 更多音视频编解码器支持
- [ ] macOS / Linux 平台适配
